pipeline {
    agent any

    stages {
        stage('stage') {
            steps {
                echo 'staging...'
            }
        }

        stage('commit') {
            steps {
                echo 'commiting...'
            }
        }

        stage('production') {
            steps {
                echo 'production...'
            }
        }
    }
}
